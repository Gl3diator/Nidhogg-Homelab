# Replace with the Nidhogg Tailscale IPv4 address.
TAILSCALE_IP=100.64.0.1

# Secrets. Never commit real values.
APP_SECRET=replace-with-secret
DB_PASSWORD=replace-with-secret
DB_ROOT_PASSWORD=replace-with-secret
